version: '3.8'

services:
  web:
    build: .
    restart: always
    ports:
      - "80:8000" # Mapea puerto 80 del EC2 al 8000 del contenedor
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"